# updated: 2026-04-22
table: organizations
source:
  schema: synthea
  table: synthea.organizations
  format: csv
  path: data/raw/organizations.csv
schema_ref: contracts/organizations.yml
output_path: warehouse/{env}/bronze/synthea_organizations/
metadata_columns:
  - ds
  - _ingested_at
  - _source_batch_id
empty_input_behavior: fail
dq_rules_table: organizations
se_action_if_failed: fail
quarantine_path: warehouse/{env}/quarantine/bronze/organizations/
timeout_minutes: 30
retries: 3
retry_delay_seconds: 60
