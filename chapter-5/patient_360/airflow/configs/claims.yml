# Bronze ingestion config for synthea.claims
# Generated by /developer-plugin:create-ingestion (story EPIC-02 / STORY-02-003)
# LLD §5.1, Decision 7 (one YAML per table), Decision 11 (empty-input
# default + override), Decision 15 (UC saveAsTable target).

table: claims                                   # filename stem; UC table = synthea_claims
target: unity.bronze.synthea_claims             # UC-managed write target [Decision 15]

source:
  schema: synthea
  table: claims
  format: duckdb

schema_ref: contracts/synthea_claims.yml         # StructType contract path

output_path: warehouse/{env}/bronze/synthea_claims/  # Delta table root (LLD §3.2)

metadata_columns:
  - ds
  - _ingested_at
  - _source_batch_id

empty_input_behavior: write_empty                    # LLD §5.1 — critical tables override to 'fail'

dq_rules_table: synthea_claims                   # convention discovery — dq_rules/synthea_claims.yml
se_action_if_failed: drop                 # fail-closed default
quarantine_path: warehouse/{env}/quarantine/bronze/synthea_claims/

# LLD §4.2 / §8.1 retry policy (bronze)
timeout_minutes: 60
retries: 3
retry_delay_seconds: 60
