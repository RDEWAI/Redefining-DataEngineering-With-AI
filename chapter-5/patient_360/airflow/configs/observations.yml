# updated: 2026-04-22
table: observations
source:
  schema: synthea
  table: synthea.observations
  format: csv
  path: data/raw/observations.csv
schema_ref: contracts/observations.yml
output_path: warehouse/{env}/bronze/synthea_observations/
metadata_columns:
  - ds
  - _ingested_at
  - _source_batch_id
empty_input_behavior: write_empty
dq_rules_table: observations
se_action_if_failed: drop
quarantine_path: warehouse/{env}/quarantine/bronze/observations/
timeout_minutes: 30
retries: 3
retry_delay_seconds: 60
