# Bronze ingestion config -- ingest_providers
# Generated for STORY-02-003 (LLD §5.1, §4.2, §7.1).
# Filename stem `providers` drives the Airflow task id `ingest_providers`
# via the factory; the `table:` field below is the Bronze Delta table
# name (synthea_providers) used to look up contracts/<table>.yml and
# dq_rules/<table>.yml.
table: synthea_providers
layer: bronze
output_path: ${PATIENT360_WAREHOUSE_ROOT}/dev/bronze/synthea_providers/

source:
  type: csv
  path: data/raw_delta/providers.csv
  header: true

schema_ref: contracts/synthea_providers.yml
contracts_dir: contracts

output_table: unity.bronze.synthea_providers
catalog_bronze_catalog_name: unity
catalog_bronze_schema: bronze

metadata_columns:
  - ds
  - _ingested_at
  - _source_batch_id

empty_input_behavior: fail            # LLD §5.1 -- FK dimension
dq_rules_table: synthea_providers
dq_rules_dir: dq_rules
se_action_if_failed: fail             # LLD §5.4 fail-closed default
quarantine_path: warehouse/{env}/quarantine/bronze/synthea_providers/

timeout_minutes: 30                   # LLD §4.2
retries: 3
retry_delay_seconds: 60
