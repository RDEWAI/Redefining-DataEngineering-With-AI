# Bronze ingestion config for synthea.careplans
# Generated by /developer-plugin:create-ingestion (story EPIC-02 / STORY-02-003)
# LLD §5.1, Decision 7 (one YAML per table), Decision 11 (empty-input
# default + override), Decision 15 (UC saveAsTable target).

table: careplans                                   # filename stem; UC table = synthea_careplans
target: unity.bronze.synthea_careplans             # UC-managed write target [Decision 15]

source:
  schema: synthea
  table: careplans
  format: duckdb

schema_ref: contracts/synthea_careplans.yml         # StructType contract path

output_path: warehouse/{env}/bronze/synthea_careplans/  # Delta table root (LLD §3.2)

metadata_columns:
  - ds
  - _ingested_at
  - _source_batch_id

empty_input_behavior: write_empty                    # LLD §5.1 — critical tables override to 'fail'

dq_rules_table: synthea_careplans                   # convention discovery — dq_rules/synthea_careplans.yml
se_action_if_failed: drop                 # fail-closed default
quarantine_path: warehouse/{env}/quarantine/bronze/synthea_careplans/

# LLD §4.2 / §8.1 retry policy (bronze)
timeout_minutes: 60
retries: 3
retry_delay_seconds: 60
