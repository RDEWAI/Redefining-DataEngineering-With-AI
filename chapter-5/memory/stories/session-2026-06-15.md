# Session 2026-06-15 — update-stories

## Trigger
User: "update-stories to have dag updates in both silver and bronze layer."
Chosen approach (AskUserQuestion): **Mirror Bronze pattern** — add dedicated Silver DAG-wiring build stories + cross-ref note on Bronze STORY-02-006.

## Gap found
The Silver integration-test stories (STORY-03-006, STORY-04-012) trigger `patient360_hourly_v1`
and assert the `transform_*_silver` tasks run, but **no build story wired those tasks into the
DAG**. Bronze had STORY-02-006 for exactly this; Silver had no equivalent. Only `reconciliation_silver`
(STORY-04-010) touched the DAG. LLD §4.2/§4.3 clearly define the `silver_dimensions` and
`silver_facts` task groups + their dependency edges, so the wiring was specified but never storied.

## Scenario
Scenario B (same version, different date) — v1.7 → v1.8. New file `BACKLOG-2026-06-15-patient-360.md`;
prior file archived to `BACKLOG-2026-05-12-patient-360.md.bak`.

## Changes
1. **NEW STORY-03-007** (build, EPIC-03, 3 pts, Sprint 6) — wire `silver_dimensions` TaskGroup:
   4 `transform_*_silver` SCD2 dim tasks (SparkSubmitOperator) downstream of `reconciliation_bronze`.
   Deps: STORY-02-006 (DAG exists) + STORY-03-001..004.
2. **NEW STORY-04-013** (build, EPIC-04, 3 pts, Sprint 7) — wire `silver_facts` TaskGroup:
   9 `transform_*_silver` fact tasks with encounters as join hub (encounters ← patients/orgs/providers;
   6 facts + careplans ← encounters; allergies ← patients). Deps: STORY-03-007 + STORY-04-001..009.
3. **Ripple wiring**: STORY-03-006 (silver-dim integration-test) deps += STORY-03-007;
   STORY-04-010 (reconciliation_silver) deps += STORY-04-013.
4. **Bronze STORY-02-006** Technical Notes: documented `patient360_hourly_v1.py` as the single
   shared full-pipeline DAG extended layer-by-layer (Bronze → Silver dims → Silver facts → Gold).
5. **BACKLOG index**: exec summary + Epic Overview (EPIC-03 6→7/27→30, EPIC-04 12→13/42→45,
   total 53→55 / 208→214), Sprint Plan (S6 12→15, S7 37→40), Version History 1.8, metadata bump.

## Preserved
- All existing ACs and stories untouched (only dependency cells extended; nothing removed).
- Closure sequences intact: perf → integration-test ordering unchanged (CLOSURE checks still pass).

## Validation
`validate_stories.py --all outputs/stories/v1` — **All checks passed. No issues found.**
(Ran via project venv `python3`; `uv run` is on the user's deny list.)

## Notes
- Pre-existing pending learning in learnings-queue.jsonl (2026-05-11, STORY-02-005 dq_rules naming)
  is unrelated to this session and was already applied in v1.3 — left untouched; apply-learnings skill
  not callable in this session.

## Follow-up — Gold DAG wiring (same session, v1.8 → v1.9, Scenario C in-place)
User: "update-stories add dag updates for gold as well."
Same gap in EPIC-05, **plus** Gold had no `reconciliation_gold` story at all (Silver had STORY-04-010).
- **NEW STORY-05-006** (build, EPIC-05, 3 pts, Sprint 8) — mirrors Bronze STORY-02-006's combined
  "TaskGroup + reconciliation" form: wire `gold_build` TaskGroup (3 `build_*_gold` SparkSubmitOperator
  tasks downstream of `reconciliation_silver`) + `reconciliation_gold` (downstream of all 3, reuses
  `utils/reconciliation.py` layer='gold', DQS §4: silver→gold row count, patient completeness=5,767,
  allergy completeness DQ-FLD-138). Deps: STORY-04-010 + STORY-05-001..003. Per LLD §4.2/§4.3/§5.5.
- **Ripple**: STORY-05-005 (gold integration-test) deps += STORY-05-006.
- EPIC-05 5→6 stories / 22→25 pts; total 55→56 / 214→217; Sprint 8 22→25.
- Scenario C (file already dated today) → in-place, v1.8 → v1.9, no new .bak.
- `validate_stories.py --all` — All checks passed.

## Status
BACKLOG status: Updated - Pending Review (v1.9). All three medallion layers (Bronze/Silver/Gold)
now have explicit DAG-wiring build stories feeding the single `patient360_hourly_v1` DAG.
Awaiting review before downstream (developer-plugin) re-cut.
