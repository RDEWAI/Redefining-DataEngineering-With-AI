# Session 2026-05-22 — update-stories

## Request
Add ONE new acceptance criterion to STORY-01-007 mandating that
`Dockerfile.airflow` makes `/opt/patient_360/` writable by the airflow
runtime user (uid 50000) BEFORE the final `USER airflow` directive switches
away from root. Retrofit for `chapter-6/developer-plugin/LLD-DEVIATIONS.md`
row 7 (direct-edit hot-fix applied 2026-05-22).

## Impact Assessment
| Changed Item | Affected Epics | Affected Stories | Sprint Impact |
|---|---|---|---|
| New AC on existing story (no scope/dependency change) | EPIC-01 | STORY-01-007 | Sprint 2: no point change |

No epic-objective, dependency, sprint allocation, point total, or status
changes. Closure-sequence (perf/int-test/deploy) untouched. Only AC count
on STORY-01-007 went from 7 → 8.

## Changes Applied
1. `outputs/stories/v1/EPIC-01-foundation-infrastructure/STORY-01-007-docker-compose-airflow-otel.md`
   - Added AC8: Dockerfile.airflow `USER root` block after JDK install but
     before `USER airflow` pip install must run
     `mkdir -p /opt/patient_360/warehouse && chown -R airflow:root /opt/patient_360 && chmod 775 /opt/patient_360`.
   - Added matching Verification AC8: three greps on `Dockerfile.airflow`
     for `mkdir -p /opt/patient_360/warehouse`,
     `chown -R airflow:root /opt/patient_360`, and `chmod 775 /opt/patient_360`.
2. `outputs/stories/v1/BACKLOG-2026-05-22-patient-360.md` (new, Scenario B)
   - Version bumped 1.8 → 1.9, Last Modified 2026-05-22,
     Status reset to `Updated - Pending Review`.
   - Appended v1.9 row in Version History describing the single-AC addition
     and citing LLD-DEVIATIONS row 7.
   - Prior `BACKLOG-2026-05-20-patient-360.md` archived as `.bak`.

## Validation
`uv run python scrum-master-plugin/skills/validate-stories/scripts/validate_stories.py --all outputs/stories/v1`
→ All checks passed. No issues found.

## Learnings Queue
One pending entry appended to `memory/stories/learnings-queue.jsonl`
capturing the rule: foundation stories that produce a Dockerfile hosting
a project root the airflow uid 50000 must write to MUST carry an explicit
AC requiring a `USER root` `mkdir + chown + chmod` block before the final
`USER airflow` directive.
