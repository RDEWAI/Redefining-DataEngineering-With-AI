{"skill": "create-scaffold", "date": "2026-04-27", "correction": "auto-mode: skipped JDK 17 install preflight; only Java 1.8 available", "pattern": "When auto-mode is active and preflight finds missing JDK for Spark 4.x, scaffold-only deliverables (no Spark execution) may proceed; later create-ingestion runs will need JDK 17", "status": "pending"}
{"skill": "create-scaffold", "date": "2026-04-27", "correction": "verify_acs.py file_count uses stdlib glob which doesn't expand brace patterns like {a,b,c}", "pattern": "Update verify_acs.py file_count handler to support brace expansion (e.g. via braceexpand or wcmatch.fnmatch), OR document that scrum-master story Verification YAML must avoid brace globs and use multiple file_count entries instead", "status": "pending"}
{"skill": "implement-stories", "date": "2026-04-27", "correction": "Skill tool dispatch to forked subagents (update-scaffold) does not deliver the args parameter — subagent reports no task argument despite .skill-arg/.skill-paths being written and explicit args passed to Skill tool", "pattern": "implement-stories must verify the downstream skill received its arg before proceeding; if subagent returns empty/idle response, halt batch with CRITICAL and surface harness arg-passing bug rather than silently continuing", "status": "pending"}
