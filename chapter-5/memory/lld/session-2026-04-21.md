# LLD Session: 2026-04-21

## What Was Updated

Updated the LLD from **v1.4 → v1.5** (Scenario B: same version folder `outputs/lld/v1/`, new file date).

- **Working file**: `outputs/lld/v1/LLD-2026-04-21-patient-360.md`
- **Backup**: `outputs/lld/v1/LLD-2026-04-17-patient-360.md.bak`
- **Status**: `Updated - Pending Review`
- **Derived artifact updated**: `outputs/lld/v1/config/config-template.yaml` (added quarantine params)

## Trigger

Change brief `inputs/lld/v1/se-runner-change-brief.md` from the Developer Plugin (Bronze Ingestion team), identifying 7 gaps found during `se_runner.py` build planning. All 7 gaps applied in this session.

## Changes Applied (by LLD section)

**Metadata** — bumped Version to 1.5, Last Modified to 2026-04-21.

**§2.3 `ingestion_runner.py`** (Gap 6) — Added metadata column names paragraph: `ds` (string `YYYY-MM-DD`), `_ingested_at` (TimestampType), `_source_batch_id` (StringType `{table}:{ds}`). Noted SE rules reference these names verbatim.

**§2.3 `se_runner.py`** (Gaps 1, 2, 5, 7):
- Added `[PENDING IMPLEMENTATION]` marker to module heading.
- Updated Input signature from `DataFrame, table, action_if_failed` to `DataFrame, table (str), env (str), action_if_failed (str), dq_rules_dir (Path)`.
- Added `dq_env` mapping table (DEV→DEV, STAGING→QA, PROD→PROD).
- Added Bootstrap mode note: soft-import logs WARNING and passes data through; fallback MUST be removed when `se_runner.py` is shipped.

**§2.3 `reconciliation.py`** (Gap 7) — Added `[PENDING IMPLEMENTATION]` marker.

**§3 Storage Layout** (Gap 4) — Added `warehouse/{env}/quarantine/bronze/{table}/` directory entry.

**§5.4 Inline SE Validation** (Gaps 1, 3):
- Updated `drop` action quarantine path from `dead-letter` to `warehouse/{env}/quarantine/bronze/{table}/`.
- Added `se_action_if_failed` semantics sentence: per-table YAML value is fail-closed default; per-rule declarations take precedence.
- Added `dq_env` mapping table with cross-reference to §2.3 `se_runner.py`.

**§7 Configuration Schema** (Gap 4):
- Added `storage.quarantine_path` parameter (per-env, default `warehouse/dev/quarantine`).
- Added `ingestion.quarantine_path` parameter (per-env, resolved at runtime by `se_runner.py`).
- Clarified `storage.dead_letter_path` description (schema/FK rejections, not SE drop-action).

**§8 Error Handling** (Gap 5):
- Added §8.5 SE Bootstrap Mode Degradation table (WARNING in bootstrap / CRITICAL post-implementation).
- Added action-required note to remove soft-import fallback after `se_runner.py` ships.
- Renumbered former §8.5 Circuit Breaker Patterns → §8.6.

**§13 Decision Log** (Gap 7) — Added Decision 14: `se_runner.py` and `reconciliation.py` pending implementation open item, with 4-item STAGING promotion checklist.

**§14 Version History** — Added v1.5 entry.

**`config/config-template.yaml`** — Added `storage_quarantine_path` and `ingestion_quarantine_path` to all three environment blocks (DEV, STAGING, PROD). Fixed `storage_dead_letter_path` comment.

## Validation

Final result: `All checks passed. No issues found.` (0 critical, 0 warnings, 0 info)

## Implementation Decisions Changed

- **Quarantine path clarified**: §3 now distinguishes `dead-letter/` (schema/FK rejects) from `quarantine/bronze/{table}/` (SE drop-action rejects). These were previously conflated.
- **`se_action_if_failed` semantics**: Fail-closed default (not override) — per-rule declarations take precedence.
- **Bootstrap mode**: Documented as explicitly temporary — removal is a pre-STAGING requirement.

## Upstream Artifacts Consumed

All upstream artifacts unchanged (same as v1.4 session).

## Open Items

- `se_runner.py` NOT YET IMPLEMENTED — Bronze runs in bootstrap mode (no DQ validation). Must be built before STAGING promotion.
- `reconciliation.py` NOT YET IMPLEMENTED — Reconciliation tasks will fail until this is built.
- Decision 14 (§13) tracks these as explicit open items with a STAGING promotion checklist.
- Prior open items from HLD §8 remain (cloud platform, production RTO/RPO, schema evolution).
- `dq_rules/{table}.yml` per-table files not yet verified to exist for all 13 tables (carry-over from v1.4).

## No Pending Learnings

No user corrections occurred during this session (mechanical application of a precise change brief).
