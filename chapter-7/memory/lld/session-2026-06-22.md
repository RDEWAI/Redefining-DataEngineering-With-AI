# LLD Update Session — 2026-06-22

## What was updated
- File: `outputs/lld/v1/LLD-2026-06-20-patient-360.md`
- Version: 1.24 → 1.25 (Scenario C in-place minor bump)
- Status: Approved → Updated - Pending Review
- Last Modified: 2026-06-20 → 2026-06-22

## Changes made
- §6.1 Compute & Local Executor Mode: added a **"Build-time Ivy/Maven JAR
  pre-resolution (Airflow worker image)"** paragraph requiring the Airflow
  worker image to pre-resolve three Spark JAR coordinates into
  `/home/airflow/.ivy2` at BUILD time:
  - `io.delta:delta-spark_2.13:4.3.0`
  - `io.unitycatalog:unitycatalog-spark_4.1_2.13:0.5.0`
  - `io.openlineage:openlineage-spark_2.13:1.50.0`
- Rationale captured inline: parallel Bronze SparkSubmit tasks (STAGING=8,
  PROD=16 per §4.1) race on a cold `~/.ivy2` cache (`unresolved dependency`).
  Build-time warm-up makes runtime `--packages` resolution offline + race-free.
- Coordinate list pinned in lockstep with §6.1 / `library-imports.yaml` /
  `spark_submit_wrapper.py` DEFAULT_PACKAGES.
- Added a "Scaffold sync reminder" callout (re-run
  `developer-plugin:create-scaffold` so `Dockerfile.airflow` renders the
  build-time Ivy warm-up step), mirroring the v1.23 pattern.
- Version-history row for 1.25 added.

## Out of scope (untouched, per skill-arg)
- No version pins changed; §9.1.1 image-table rows, DAG, configs, catalog
  wiring, and DMS/STM/DQS left untouched. Chapter-5 not touched.

## Downstream action
- Re-run `developer-plugin:create-scaffold` so `_infra/docker/Dockerfile.airflow`
  renders the build-time Ivy warm-up step.
- Re-approval required (Status is Pending Review) before downstream stories re-cut.

## Remaining open items
- 1 pending correction in `memory/lld/learnings-queue.jsonl` (apply-learnings).
