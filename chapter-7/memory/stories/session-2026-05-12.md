# Session 2026-05-12 — update-stories

## Trigger
LLD bumped v1.11 → v1.12 with 8 architectural pivots (2026-05-12 debug session). Update Epic-02 (and any other affected) story ACs to match.

## Scenario
Scenario B (same version, different date) — v1.6 → v1.7. New file `BACKLOG-2026-05-12-patient-360.md`; prior file archived to `BACKLOG-2026-05-11-patient-360.md.bak`.

## Pivots applied
1. **3-part `unity.bronze.<table>` FQN → path-based Delta under `warehouse/{env}/bronze/<table>/`** — STORY-02-001 (runner write contract + forbidden_grep), STORY-02-003 (configs `output_path` + forbidden `output_table: unity.bronze.*`), STORY-02-008 (integration asserts Delta paths, not UC tables).
2. **STORY-02-006 reconciliation_bronze → SparkSubmitOperator** (was PythonOperator); DAG defaults `max_active_tasks=1`, `catchup=False` per LLD §4.1 DEV.
3. **STORY-02-007 DEV compute → 1g driver / 1g executor** (was 2g/2g) per LLD §6.1.
4. **STORY-02-009 deploy-validation** — forbid UC-managed write assertions; verify path-based Delta + `_delta_log/`.
5. **STORY-02-001 + STORY-02-003 source.type=csv default**; DuckDB allowed for ≤ 6 small reference tables per LLD §5.1 source-selection rule.
6. **STORY-02-008 SE evidence query → meta_dq_run_date only**; forbidden_grep on `meta_dq_run_id = ts_nodash` / `{run_id}` clauses.
7. **EPIC-01 scaffold (STORY-01-001 / -01-002 / -01-007)** — DeltaCatalog + Hive metastore (Derby) with persistent JDBC URL; `UCSingleCatalog` forbidden; `PATIENT360_PROJECT_ROOT` exported by docker-compose for every Airflow service.

## Preserved
- All already-checked ACs that remain correct (cookiecutter scaffold render, docker-compose service blocks, smoke/probe evidence, otel-collector distroless rule).
- No stories added or removed; 53 stories / 208 pts unchanged.

## Validation
`scrum-master-plugin:validate-stories` — all checks passed (0 critical, 0 warnings).

## Status
BACKLOG status: Updated - Pending Review. Awaiting re-approval before downstream re-cut.
