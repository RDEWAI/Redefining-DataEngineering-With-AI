# Session Notes: 2026-03-15

## What Was Created
- **HLD**: `outputs/hld/v1/HLD-2026-03-15-patient-360-pipeline.md` (v1.0)
- **Status**: Draft — Pending Review
- **Validation**: PASSED (0 critical, 0 warnings, 1 info — 6 Phase 2 placeholders)

## Architecture Pattern Selected
- **Medallion (Bronze/Silver/Gold)** with batch processing
- Justification: Team proficient, DRD SLAs satisfied (1hr clinical, 24hr billing), no streaming requirement at query level, infrastructure is local Docker only

## Key Design Decisions
1. **Medallion over Lambda/Kappa/Data Vault** — team has no streaming experience; DRD requires 1hr max latency, not sub-minute
2. **Delta Lake 4.1.0** — mandated by infrastructure constraints; team proficient
3. **Unity Catalog OSS 0.4.0** — in approved tech catalog; catalog name = `spark_catalog`
4. **SCD Type 2** for 4 dimension tables (patients, providers, payers, organizations)
5. **Full Snapshot CDC** for all 13 tables — no `updated_at` columns in source (verified via DB query)
6. **3 Gold tables** — patient_summary (clinical), patient_clinical_history (timeline), patient_billing_summary (finance)
7. **Hourly batch** for medications & allergies (Phase 1 compromise vs DRD sub-minute sync)
8. **4h RTO / 24h RPO** for Phase 1 (read-only system, source EHR is authoritative)
9. **No encryption in local dev** — document Phase 2 production requirements

## Database Verification
- Database path: `data/duckdb/raw.db` (636 MB)
- 18 tables in `synthea` schema; 13 in Phase 1 scope
- Row counts match DRD §2.3 verified values exactly
- No `updated_at`/`modified_at` columns found — confirms Full Snapshot CDC

## DRD Gaps Found
- DRD §7.6 DR requirements marked as TBD — HLD documents 4h/24h for Phase 1 per user decision
- DRD §2.2 marks claims_transactions, payer_transitions, devices, supplies, imaging_studies as Phase 2

## Open Questions (8 total)
1. Sub-minute CDC path for meds/allergies in Phase 2
2. Cloud platform selection (Databricks/EMR/Dataproc)
3. Production RTO/RPO targets
4. Schema evolution strategy (mergeSchema vs versioned)
5. Column-level lineage strategy (DataHub vs Databricks UC)
6. Fuzzy name matching implementation (from DRD)
7. Deceased patient retention policy (from DRD)
8. Pin Marquez version for reproducibility
