# Data Modeler Session — 2026-03-18

## What Was Created

**DMS file**: `outputs/dms/v1/DMS-2026-03-18-patient-360.md` (v1.0)
**Validation**: All checks passed — 0 critical, 0 warnings, 0 info

## Key Schema Decisions

### Bronze Layer (13 tables)
- Schema: `bronze`, naming: `synthea_{table}`, partition by `ds DATE`, write: `replaceWhere`
- All source columns preserved exactly as-is with `_ingested_at`, `_source_batch_id`, `_source_file` added

### Silver Layer (13 tables across 3 schemas)
- `clinical` schema: 9 clinical entity tables
- `reference` schema: 3 SCD Type 2 dimension tables (organizations, providers, payers)
- `billing` schema: billing_claims (restricted access)
- SCD Type 2 tables: Delta MERGE INTO, no ds partition
- Fact tables: replaceWhere on ds

### Gold Layer (3 tables in `analytics` schema)
- `patient_summary`: one row per current patient (SCD2 is_current=true)
- `patient_clinical_history`: one row per encounter (340K rows)
- `patient_billing_summary`: one row per encounter with cost fields (billing-only)
- Gold naming uses `patient_{scope}` NOT `dim_`/`fact_` — denormalized consumer tables per HLD Decision 5

### SCD Type Decisions
- **Type 2**: name, address, marital_status, healthcare financials on clinical_patients; name/address on orgs; name/specialty on providers; name/ownership on payers
- **Type 1** (overwrite): race, ethnicity, gender on clinical_patients — corrections not history per governance policy §6

### Complex Type in Gold
- `patient_summary.allergies`: `ARRAY<STRUCT<description VARCHAR, severity VARCHAR>>`
- Rationale: eliminates join to clinical_allergies for 2-second search SLA [DRD §4.3]
- Enterprise data dictionary updated to allow complex types in Gold layer only

## Source Data Observations

- `allergies.STOP` is `VARCHAR` in source (not DATE) — explicit cast required at Silver
- All cost columns (BASE_ENCOUNTER_COST, TOTALCOST, OUTSTANDING*, REVENUE, etc.) are `DOUBLE` in source — cast to `DECIMAL(12,2)` in Silver
- SNOMED CODE columns in conditions, medications, allergies, procedures, careplans are `BIGINT` — cast to `VARCHAR` in Silver
- `observations.CODE` is already `VARCHAR` (LOINC codes)
- `immunizations.CODE` is already `VARCHAR` (CVX codes)
- `providers.SPECIALITY` contains a typo — Silver corrects to `specialty` (open question #4)
- No natural ID column in conditions, observations, allergies, immunizations, procedures — composite PKs used in Silver

## Row Counts (Verified Against source DuckDB)
- patients: 5,767 | encounters: 340,532 | conditions: 209,767
- medications: 290,136 | observations: 4,366,447 | procedures: 946,498
- allergies: 5,607 | immunizations: 82,605 | claims: 630,668
- careplans: 19,478 | organizations: 1,080 | providers: 1,080 | payers: 10

## HLD Gaps Found
- None — HLD §4.3, §4.5, and §2.1 provided complete layer and table specifications

## Open Questions (from DMS §10)
1. ARRAY<STRUCT> for conditions in patient_clinical_history (count vs. descriptions)
2. claims.PRIMARYPATIENTINSURANCEID references payer transitions, not payer ID — Phase 2 dependency
3. Observation detail in Gold (counts only now — specific lab values need scoped addition)
4. providers.SPECIALITY typo handling in Bronze vs Silver
