## LLD Approval Session — 2026-05-20

- **Action**: Approved LLD
- **File**: chapter-4/outputs/lld/v1/LLD-2026-05-19-patient-360.md
- **Version**: 1.13
- **Previous Status**: Updated - Pending Review
- **New Status**: Approved

---

## LLD Update Session — 2026-05-20 (later same day)

- **Action**: update-lld (Scenario B — same v1, new date)
- **File (new)**: chapter-4/outputs/lld/v1/LLD-2026-05-20-patient-360.md
- **File (archived)**: chapter-4/outputs/lld/v1/LLD-2026-05-19-patient-360.md.bak
- **Version**: 1.13 → 1.14
- **Status**: Approved → Updated - Pending Review

### Changes made
- **§4.2 Task Inventory** — added new root task `bootstrap_se` (SparkSubmitOperator) upstream of the entire `bronze_ingestion` TaskGroup. Spec: `application=scripts/bootstrap_se_tables.py` (resolved via `SE_BOOTSTRAP_APP`, default `/opt/patient_360/scripts/bootstrap_se_tables.py`), `conn_id=spark_default`, 1g driver / 1g executor / 1 core / 1 executor, `retries=1`, `retry_delay=60s`, `execution_timeout=5m`, idempotent `CREATE SCHEMA IF NOT EXISTS bronze` + `CREATE TABLE IF NOT EXISTS bronze.bronze_se_stats` (and `bronze_se_errors`). Added a rationale paragraph immediately above the Bronze TaskGroup callout citing LLD-DEVIATIONS row 6 and the 2026-05-20 `DELTA_CREATE_TABLE_WITH_NON_EMPTY_LOCATION` regression.
- **§4.2 Bronze TaskGroup rows** — all 13 ingest rows changed `Dependencies: None` → `Dependencies: bootstrap_se`.
- **§4.1 Airflow DAG Configuration** — `max_active_tasks` / `concurrency` DEV value restored from the temporary workaround `1` back to the originally specified `16`. Rationale cell updated to credit the new `bootstrap_se` task for closing the CREATE-race that motivated the workaround.
- **§4.3 DAG diagram (Mermaid)** — added `BS[bootstrap_se]` node and `BS --> B01..B13` edge.
- **§14 Version History** — added v1.14 row with full change rationale.

### Derived artifacts updated
- `outputs/lld/v1/dag/dag-definition.yaml` — new `bootstrap_se` task entry; all 13 bronze ingestion task `dependencies` set to `[bootstrap_se]`; header comment bumped to LLD-2026-05-20; concurrency string updated to `DEV: 16; STAGING: 8; PROD: 16`.
- `outputs/lld/v1/dag/dag-pipeline.mmd` — added `BS[bootstrap_se]` + `BS --> B01..B13` edge.
- `outputs/lld/v1/dag/dag-diagram.mmd` — same diagram update plus a `classDef boot` style for the new node.

### Decisions
- §13 untouched per user instruction. The existing Decision 16 (SE Execution & Run-Evidence Are Mandatory) and the 2026-05-12 Decision 12/15 revocations remain valid. `bootstrap_se` is a fulfillment of Decision 16's intent and does not introduce a new decision.

### Open items
- The new `bootstrap_se` task must be implemented in the Airflow DAG factory (chapter-6 developer plugin). The DAG factory pattern (DagContext.get_current TaskGroup approach) is unaffected — `bootstrap_se` is a top-level task, not inside the TaskGroup.
- LLD-DEVIATIONS row 6 already marks itself Resolved (2026-05-20). No further deviation logging needed.
- Status is `Updated - Pending Review` — re-approval is required via `/technical-lead-plugin:approve-lld` before downstream chapter-6 story dispatch consumes the new task spec.

### Reminder — downstream regeneration
- The §4.2 task-type set changed (added `Bootstrap` layer with `SparkSubmitOperator` task type). Per the create-lld §3c convention, run `/developer-plugin:create-dag` to regenerate `airflow/jobs/run_bootstrap.py` (if any wrapper is needed) and verify `_infra/docker/docker-compose.yml` has the `SE_BOOTSTRAP_APP` env var bound. The actual bootstrap script `scripts/bootstrap_se_tables.py` is already in place per LLD-DEVIATIONS row 6.

---

## LLD Approval Session — 2026-05-20 (v1.14)

- **Action**: Approved LLD (approve-lld)
- **File**: chapter-4/outputs/lld/v1/LLD-2026-05-20-patient-360.md
- **Version**: 1.14
- **Previous Status**: Updated - Pending Review
- **New Status**: Approved
- **Note**: Downstream chapter-6 story dispatch may now consume the new `bootstrap_se` task spec from §4.2.
