# LLD Update Session — 2026-05-12

## What was updated
- File: `outputs/lld/v1/LLD-2026-05-12-patient-360.md` (v1.11 → v1.12, Scenario B)
- Previous file archived: `LLD-2026-05-11-patient-360.md.bak`
- Status reset: Approved → Updated - Pending Review

## Changes made (7 architectural pivots from 2026-05-12 debug session)

1. **§13 Decision 12 revoked & replaced.** UCSingleCatalog as Spark catalog
   is incompatible with Airflow 3.x embedded Spark on local-FS — reverted to
   default `spark_catalog` + Delta + Hive metastore on Derby. UC OSS server +
   UI remain in compose for UI-demo only.
2. **§13 Decision 15 revoked.** With UC removed from the runtime path, Bronze
   writes revert to path-based Delta against `warehouse/{env}/bronze/<table>/`.
   All 13 Bronze rows in §5.1 Output column flipped.
3. **§5.1 source-selection rule.** Bronze default = CSV; DuckDB only for
   tables < 100 MB raw CSV (organizations, providers, payers, careplans,
   allergies, immunizations).
4. **§2.3 column-contract source.** Bronze contracts derived from source
   (DuckDB `DESCRIBE`, CSV header). DMS owns Silver/Gold contracts only.
5. **§4.1 DEV defaults.** `max_active_tasks=1`, `catchup=False`.
6. **§4.2 Spark mandate.** Any task touching Spark MUST use SparkSubmitOperator.
7. **§6.1 DEV memory.** 1g driver / 1g executor (was 2g/2g — OOM in 8GB Docker).
   §7 default `compute.spark_driver_memory`/`spark_executor_memory` bumped to `1g`.
8. **§9.1 new prose.** `PATIENT360_PROJECT_ROOT` anchors all relative paths;
   docker-compose env exports it.
9. **§8.6.1 SE evidence query.** Filter on `meta_dq_run_date` only — SE
   generates its own `meta_dq_run_id` and rejects any Airflow `ts_nodash`
   override.

## Validator change
- `check_bronze_uc_wiring` in `technical-lead-plugin/skills/validate-lld/scripts/validate_lld.py`
  now honors an explicit Decision 12/15 revocation marker (skips the rule
  when either decision carries `Revoked` / `Reverted` status). Required to
  let path-based Bronze writes pass after Decision 15 was revoked.

## Downstream regeneration reminders
- **Run `/developer-plugin:create-ingestion`** to regenerate
  `src/patient_360/bronze/ingestion_runner.py` against the new path-based
  contract (Decision 15 revoked).
- **Update `_infra/docker/Dockerfile.airflow` / `docker-compose.yml`** to
  export `PATIENT360_PROJECT_ROOT` and adopt the 1g/1g Spark sizing for DEV.
- **Update bootstrap story AC** in `outputs/stories/v1/EPIC-01-*/STORY-01-NNN-bootstrap*.md`
  if the spark-submit smoke references the old 2g/2g or UC-managed Bronze
  output target.
- **Downstream `developer-plugin` rule `UC-WIRING-001` is now retired** in
  lock-step with this LLD revision.

## Open items
- Re-approval required before downstream stories re-cut.
- The `unity-catalog` / `unity-catalog-ui` compose services remain for UI
  demo; if a future revision drops them entirely, §9.1.1 needs another pass.

## Traceability notes
- §11 Upstream references unchanged (DRD/HLD/DMS/STM/DQS section pointers
  still valid).
- §12 Traceability matrix unchanged (requirement → implementation mapping
  still holds; only the implementation's catalog wiring shifted under it).
