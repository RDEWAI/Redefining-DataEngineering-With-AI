# LLD Update Session — 2026-04-23

## What was updated

- File: `outputs/lld/v1/LLD-2026-04-23-patient-360.md`
- Version: 1.5 → **1.6**
- Scenario: **B** (same version folder v1; new date 2026-04-23; prior file `LLD-2026-04-21-patient-360.md` renamed to `.bak`)
- Status: Updated - Pending Review

## Changes made

Rebuilt §8 Error Handling around the four failure classes per the updated
create-lld / update-lld / validate-lld skills (Apr 23 change).

- **§8 intro** — added the four-failure-classes routing table (transient, row_dq,
  agg_dq/query_dq, ingest DLQ). Clarified that "dead letter" refers ONLY to the
  ingest DLQ; SE owns the `_error` path.
- **§8.1 Retry Policies** — preserved retry table; expanded drop/fail/**ignore**
  semantics for `action_if_failed`. Merged the former §8.6 Circuit Breaker
  Patterns (UC OSS, Marquez, Grafana) into §8.1 as a subsection.
- **§8.2 SE `_error` Table (row_dq)** — NEW. Named the SE-managed
  `<target>_error` Delta table created automatically via
  `se.enable.error.table=true`. Documented SE metadata columns
  (`meta_dq_run_id`, `dq_rules`, `row_dq_error_threshold_exceeded`, etc.),
  per-layer retention, naming convention across DEV/QA/PROD, and anti-pattern
  (no parallel writer for row_dq drops).
- **§8.3 SE Stats & Detailed Tables (agg_dq, query_dq)** — NEW. Named
  `{layer}_se_stats` stats tables plus `*_agg_dq_detailed` / `*_query_dq_detailed`
  tables; documented `se.enable.stats.table` and detailed-result flags;
  described monitoring consumers.
- **§8.4 Ingest DLQ (pre-validation only)** — rewrote to cover ONLY file-level
  parse, missing required columns, pre-validation cast failures, and encoding
  errors. Added explicit "what does NOT go here" exclusion list (row_dq drops
  go to §8.2, agg/query_dq outcomes to §8.3, FK orphans to SE `_error`).
  Kept `warehouse/{env}/dead-letter/{table}/{ds}/` Parquet path and schema.
- **§8.5 Alerting Thresholds** — expanded to route alerts by failure class.
  Added SE error drop threshold rule, agg_dq breach, ingest DLQ row-count
  thresholds, and bootstrap-mode-in-STAGING/PROD critical alert.
- **§8.6 SE Bootstrap Mode Degradation** — preserved, renumbered from §8.5.
- **§8.7 Escalation Path** — preserved, renumbered from §8.4.

### Removed anti-pattern

- Old **§8.2 Dead Letter / Quarantine Strategy** table that conflated row_dq
  rejections, FK orphan records, schema enforcement failures, and allergy DQ
  failures into a single "dead letter" Parquet path. This duplicated SE's
  built-in `_error` table. SE now owns `<target>_error`; the ingest DLQ is
  reserved for pre-validation failures only.

### Cross-section consistency updates

Fixed stale §8.x cross-references elsewhere in the document now that §8
renumbered:

- §5.4 unchanged — line 538 already references §8.1 correctly.
- §11 Upstream Artifact References:
  - "Allergy safety requirements" → §8.5 (was §8.3)
  - "Edge case handling" → §8.2 + §8.4 (was §8.2 Quarantine Strategy)
  - "Statistical distribution baselines" → §8.3 + §8.5 (was §8.3 Alerting)
  - "Alert/escalation framework" → §8.5 + §8.7 (was §8.3, §8.4)
  - "Networking & UC OSS access" → §8.1 circuit breaker (was §8.5)
- §12.1 FR-8 Allergy never suppressed → §8.5 (was §8.3)
- §12.3 Unity Catalog OSS circuit breaker → §8.1 (was §8.5)
- §13 Decision 14 context → §8.6 bootstrap mode (was §8.5)

Historical §8.x references inside Version History rows 1.3 and 1.5 were left
as-is because they describe those past updates against the §8 numbering that
existed at the time.

## Validation

- `uv run python technical-lead-plugin/skills/validate-lld/scripts/validate_lld.py
  outputs/lld/v1/LLD-2026-04-23-patient-360.md`
- Result: **All checks passed. No issues found.**
- Early iterations caught:
  1. "SE `_error` table (row_dq)" section missing — fixed by adding §8.2.
  2. Custom-writer anti-pattern regex matched my own "no custom writer" prose
     twice. Reworded without triggering the validator (the validator flags any
     mention of "custom" + "writer" near "row_dq"/"row-level DQ").

## Derived artifacts (auto-regenerated by PostToolUse hook)

- `outputs/lld/v1/config/dev.yaml` + `stage.yaml` + `prod.yaml`
- `outputs/lld/v1/dag/dag-definition.yaml` + `dag-pipeline.mmd`
- `outputs/lld/v1/impl-sequence.md`

No §7 parameters or §4 DAG structure changed in this update, so the derived
artifacts are unchanged in content (they are re-emitted by the hook on every
save).

## Open items

- Decision 14 (`se_runner.py` and `reconciliation.py` pending implementation)
  remains open. The new §8.6 cross-reference is correct. STAGING promotion
  checklist unchanged.
- §8.2 and §8.3 assume a standard SE integration (`se.enable.error.table=true`,
  `se.enable.stats.table=true`); when `se_runner.py` ships, confirm those
  config flags are passed through and the SE-managed tables land at the table
  names documented here.

## Remaining learnings queue

No new pending entries beyond whatever existed before this session. No user
corrections were issued during this update — all changes were driven by the
explicit change spec in the user's prompt.

---

## LLD Approval Session — 2026-04-23

- **Action**: Approved LLD
- **File**: `outputs/lld/v1/LLD-2026-04-23-patient-360.md`
- **Version**: 1.6
- **Previous Status**: Updated - Pending Review
- **New Status**: Approved
- **Rationale**: Unblock the create-stories approval gate. §8 Error Handling
  restructure (v1.6) validated cleanly and is ready for Scrum Master handoff.
- **Downstream impact**: Scrum Master can now run `create-stories` against
  this LLD — the upstream approval gate for Stories (DRD, HLD, DMS, STM, DQS,
  LLD) is now complete.
