# LLD Session — 2026-05-21

## What was updated
- `chapter-4/outputs/lld/v1/LLD-2026-05-21-patient-360.md` (new file, Scenario B)
- Prior `LLD-2026-05-20-patient-360.md` archived to `.bak`
- Version: 1.14 → 1.15
- Status: Approved → Updated - Pending Review

## Change driver
Contradiction between §13 Decision 12 (2026-05-12 revision selects embedded
Derby Hive metastore — single-process database) and §4.1 (2026-05-20 v1.14
raise to DEV `max_active_tasks=16`). Reproduced 2026-05-21: parallel
SparkSubmitOperator tasks fail with `ERROR XSDB6: Another instance of Derby
may have already booted the database null`. The `bootstrap_se` task only
solves the SE-table CREATE race, not Derby's concurrent-access cap.

## Edits applied
1. **§13 Decision 12** — appended "Constraints (added 2026-05-21)" paragraph
   stating the single-process Derby limit and the `max_active_tasks=1` cap.
   Cited the 2026-05-21 XSDB6 reproduction. Expanded Affected Sections list
   to include §4.1 and Decision 16 candidate.
2. **§4.1** — DEV `max_active_tasks` reverted 16 → 1 (Concurrency to match).
   Rationale cell rewritten to cite Decision 12 Constraints and the XSDB6
   reproduction; noted that the 2026-05-20 raise was based on an incomplete
   bottleneck read. STAGING (8) / PROD (16) unchanged.
3. **§4.2** — added "Note (2026-05-21 correction)" paragraph clarifying that
   `bootstrap_se` + `max_active_tasks=1` together close BOTH failure modes
   (SE-table CREATE race + Derby concurrency). Updated Bronze TaskGroup
   prose: "in DEV the 13 tasks serialize behind the Derby single-process
   cap" replaces the prior "fan out at the originally specified concurrency"
   wording. No topology change — `bootstrap_se` task entry and §4.3 DAG
   diagram preserved.
4. **§13** — added Decision 16 candidate placeholder (Postgres metastore
   migration for DEV concurrency > 1). Status candidate, not number-assigned
   yet. Pending scope items listed (cost, container choice — HMS standalone
   vs reuse `marquez-db` Postgres — UC OSS replay validation).
5. **§14** — new v1.15 row dated 2026-05-21 citing the XSDB6 reproduction
   and Decision 12 Constraints clarification.

## Upstream traceability
- No changes to DRD / HLD / DMS / STM / DQS references.
- LLD-DEVIATIONS row 6 (chapter-6/developer-plugin/LLD-DEVIATIONS.md)
  cited in the v1.15 change log.

## Validation
`uv run python technical-lead-plugin/skills/validate-lld/scripts/validate_lld.py
outputs/lld/v1/LLD-2026-05-21-patient-360.md` → all checks passed.

## Remaining open items
- Decision 16 (Postgres metastore migration) is a candidate, not formally
  numbered. Team needs to scope: cost, HMS standalone vs reuse `marquez-db`,
  UC OSS replay validation, and whether zero-config-local goal is preserved.
- Re-approval of LLD v1.15 required before downstream stories re-cut.
- DAG definition YAML and `dag/` artifacts unchanged (no topology change).
- Config templates (`outputs/lld/v1/config/`) unchanged (no §7 change).

---

## LLD Approval Session — 2026-05-21

- **Action**: Approved LLD
- **File**: `chapter-4/outputs/lld/v1/LLD-2026-05-21-patient-360.md`
- **Version**: 1.15
- **Previous Status**: Updated - Pending Review
- **New Status**: Approved
- **Edits**: Status row → Approved; new Version History row appended ("1.15 | 2026-05-21 | Technical Lead Agent | Status changed to Approved"). Last Modified already 2026-05-21; no change.
- **Downstream gate**: Stories (chapter-5 Scrum Master) can now be (re-)cut against this LLD.
