# DAG Definition: patient360_hourly_v1
# Derived from: LLD-2026-05-20-patient-360.md, Section 4 + Section 5
# Generated by: generate_dag_definition.py

dag:
  dag_id: patient360_hourly_v1
  dag_file: airflow/dags/patient360_hourly_v1.py
  config_file: airflow/configs/patient360_hourly_v1.yaml
  schedule: "0 * * * *"
  timezone: UTC
  catchup: False
  max_active_runs: 1
  concurrency: DEV: 16; STAGING: 8; PROD: 16
  default_timeout_minutes: 60

default_args:
  retries: 3
  retry_delay_seconds: 60

tasks:
  - task_id: bootstrap_se
    type: SparkSubmitOperator
    layer: bootstrap
    application: "${SE_BOOTSTRAP_APP:-/opt/patient_360/scripts/bootstrap_se_tables.py}"
    conn_id: spark_default
    driver_memory: 1g
    executor_memory: 1g
    executor_cores: 1
    num_executors: 1
    dependencies: []
    timeout_minutes: 5
    retries: 1
    retry_delay: "60s fixed"
    idempotency: "CREATE SCHEMA IF NOT EXISTS bronze; CREATE TABLE IF NOT EXISTS bronze.bronze_se_stats USING DELTA LOCATION ...; same for bronze.bronze_se_errors. No-op if both already exist."

  - task_id: bronze_ingestion.ingest_patients
    type: SparkSubmitWrapper
    layer: bronze
    module: patient_360 (cookiecutter `project_name`).bronze.bronze_ingestion.ingest_patients
    dependencies: [bootstrap_se]
    timeout_minutes: 30
    retries: 3
    retry_delay: "60s exp"

  - task_id: bronze_ingestion.ingest_encounters
    type: SparkSubmitWrapper
    layer: bronze
    module: patient_360 (cookiecutter `project_name`).bronze.bronze_ingestion.ingest_encounters
    dependencies: [bootstrap_se]
    timeout_minutes: 30
    retries: 3
    retry_delay: "60s exp"

  - task_id: bronze_ingestion.ingest_conditions
    type: SparkSubmitWrapper
    layer: bronze
    module: patient_360 (cookiecutter `project_name`).bronze.bronze_ingestion.ingest_conditions
    dependencies: [bootstrap_se]
    timeout_minutes: 30
    retries: 3
    retry_delay: "60s exp"

  - task_id: bronze_ingestion.ingest_medications
    type: SparkSubmitWrapper
    layer: bronze
    module: patient_360 (cookiecutter `project_name`).bronze.bronze_ingestion.ingest_medications
    dependencies: [bootstrap_se]
    timeout_minutes: 30
    retries: 3
    retry_delay: "60s exp"

  - task_id: bronze_ingestion.ingest_observations
    type: SparkSubmitWrapper
    layer: bronze
    module: patient_360 (cookiecutter `project_name`).bronze.bronze_ingestion.ingest_observations
    dependencies: [bootstrap_se]
    timeout_minutes: 30
    retries: 3
    retry_delay: "60s exp"

  - task_id: bronze_ingestion.ingest_allergies
    type: SparkSubmitWrapper
    layer: bronze
    module: patient_360 (cookiecutter `project_name`).bronze.bronze_ingestion.ingest_allergies
    dependencies: [bootstrap_se]
    timeout_minutes: 30
    retries: 3
    retry_delay: "60s exp"

  - task_id: bronze_ingestion.ingest_immunizations
    type: SparkSubmitWrapper
    layer: bronze
    module: patient_360 (cookiecutter `project_name`).bronze.bronze_ingestion.ingest_immunizations
    dependencies: [bootstrap_se]
    timeout_minutes: 30
    retries: 3
    retry_delay: "60s exp"

  - task_id: bronze_ingestion.ingest_procedures
    type: SparkSubmitWrapper
    layer: bronze
    module: patient_360 (cookiecutter `project_name`).bronze.bronze_ingestion.ingest_procedures
    dependencies: [bootstrap_se]
    timeout_minutes: 30
    retries: 3
    retry_delay: "60s exp"

  - task_id: bronze_ingestion.ingest_claims
    type: SparkSubmitWrapper
    layer: bronze
    module: patient_360 (cookiecutter `project_name`).bronze.bronze_ingestion.ingest_claims
    dependencies: [bootstrap_se]
    timeout_minutes: 30
    retries: 3
    retry_delay: "60s exp"

  - task_id: bronze_ingestion.ingest_careplans
    type: SparkSubmitWrapper
    layer: bronze
    module: patient_360 (cookiecutter `project_name`).bronze.bronze_ingestion.ingest_careplans
    dependencies: [bootstrap_se]
    timeout_minutes: 30
    retries: 3
    retry_delay: "60s exp"

  - task_id: bronze_ingestion.ingest_organizations
    type: SparkSubmitWrapper
    layer: bronze
    module: patient_360 (cookiecutter `project_name`).bronze.bronze_ingestion.ingest_organizations
    dependencies: [bootstrap_se]
    timeout_minutes: 30
    retries: 3
    retry_delay: "60s exp"

  - task_id: bronze_ingestion.ingest_providers
    type: SparkSubmitWrapper
    layer: bronze
    module: patient_360 (cookiecutter `project_name`).bronze.bronze_ingestion.ingest_providers
    dependencies: [bootstrap_se]
    timeout_minutes: 30
    retries: 3
    retry_delay: "60s exp"

  - task_id: bronze_ingestion.ingest_payers
    type: SparkSubmitWrapper
    layer: bronze
    module: patient_360 (cookiecutter `project_name`).bronze.bronze_ingestion.ingest_payers
    dependencies: [bootstrap_se]
    timeout_minutes: 30
    retries: 3
    retry_delay: "60s exp"

  - task_id: reconciliation_bronze
    type: SQL (query_dq)
    layer: reconciliation
    module: patient_360 (cookiecutter `project_name`).reconciliation.reconciliation_bronze
    dependencies: [All bronze_ingestion tasks]
    timeout_minutes: 20
    retries: 1
    retry_delay: "60s fixed"

critical_path:
  steps:
    - ingest_observations_bronze
    - reconciliation_bronze
    - transform_patients_silver
    - transform_encounters_silver
    - transform_observations_silver
    - reconciliation_silver
    - build_patient_summary_gold
    - reconciliation_gold
    - emit_lineage + emit_metrics
