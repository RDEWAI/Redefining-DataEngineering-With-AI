# Configuration Template: patient_360 (dev)
# Auto-generated from LLD Section 7 — Configuration Schema
# Generated by: generate_config_template.py
# Scaffold target: _infra/cd/config/dev.yaml

scaffold_target: _infra/cd/config/dev.yaml
env: dev
pipeline:
  name: patient_360
  version: "1.0"

scheduling:
  # Pipeline execution schedule
  cron_schedule: "0 */4 * * *"
  # Maximum pipeline execution time
  timeout_minutes: 30

compute:
  # Spark executor memory allocation
  executor_memory: "2g"
  # Number of Spark executors
  num_executors: 1
  # Shuffle partitions for Spark SQL
  shuffle_partitions: 8

storage:
  # Root path for warehouse data
  warehouse_path: "warehouse/dev"
  # Delta Lake file format
  file_format: "delta"
  # Compression codec
  compression: "snappy"

error_handling:
  # Maximum retry attempts for failed tasks
  max_retries: 1
  # Retry backoff in seconds
  retry_backoff_seconds: 30
  # Path for dead letter records
  dead_letter_path: "warehouse/dev/dead-letter"

monitoring:
  # Alerting channel for notifications
  alert_channel: "#data-alerts-dev"
  # Application log level
  log_level: "DEBUG"

# ── Sibling files also written by the same skill run ─────────────────────────
# stage.yaml → scaffold_target: _infra/cd/config/stage.yaml
# prod.yaml  → scaffold_target: _infra/cd/config/prod.yaml
# (The developer drops each file at its scaffold_target path in chapter-5.)
