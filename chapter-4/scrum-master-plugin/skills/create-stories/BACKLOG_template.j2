# Sprint Backlog: {{ backlog.title }}

| Field | Value |
|-------|-------|
| **Version** | {{ backlog.version }} |
| **Created** | {{ backlog.created_date }} |
| **Last Modified** | {{ backlog.modified_date }} |
| **Author** | {{ backlog.author }} |
| **Status** | {{ backlog.status }} |
| **LLD Reference** | {{ backlog.lld_reference }} |

---

## 1. Executive Summary

{{ backlog.executive_summary }}

---

## 2. Epic Overview

| Epic | Title | Stories | Points | Sprints | LLD Section |
|------|-------|---------|--------|---------|-------------|
{% for epic in backlog.epics %}
| EPIC-{{ "%02d"|format(epic.number) }} | {{ epic.title }} | {{ epic.story_count }} | {{ epic.total_points }} | {{ epic.sprint_range }} | {{ epic.lld_section }} |
{% endfor %}

**Total**: {{ backlog.total_stories }} stories, {{ backlog.total_points }} points across {{ backlog.total_sprints }} sprints

---

## 3. Dependency Graph

```mermaid
{{ backlog.dependency_diagram }}
```

---

## 4. Sprint Plan

{% for sprint in backlog.sprints %}
### Sprint {{ sprint.number }}: {{ sprint.goal }}

| Story ID | Title | Points | Epic |
|----------|-------|--------|------|
{% for story in sprint.stories %}
| {{ story.id }} | {{ story.title }} | {{ story.points }} | {{ story.epic }} |
{% endfor %}

**Sprint Total**: {{ sprint.total_points }} points
{% endfor %}

---

## 5. Traceability Matrix

| Epic / Story | LLD | DMS | STM | DQS | DRD | HLD |
|-------------|-----|-----|-----|-----|-----|-----|
{% for row in backlog.traceability %}
| {{ row.item }} | {{ row.lld }} | {{ row.dms }} | {{ row.stm }} | {{ row.dqs }} | {{ row.drd }} | {{ row.hld }} |
{% endfor %}

---

## 6. Risks & Assumptions

{% for risk in backlog.risks %}
- **{{ risk.title }}**: {{ risk.description }} _(Mitigation: {{ risk.mitigation }})_
{% endfor %}

### Assumptions

{% for assumption in backlog.assumptions %}
- {{ assumption }}
{% endfor %}

---

## 7. Version History

| Version | Date | Author | Changes |
|---------|------|--------|---------|
{% for entry in backlog.version_history %}
| {{ entry.version }} | {{ entry.date }} | {{ entry.author }} | {{ entry.changes }} |
{% endfor %}
