# Mapping Analyst Session - 2026-03-17

## Pipeline
Patient 360 Medallion Pipeline

## Output
`outputs/stm/v1/STM-2026-03-17-patient-360.xlsx`

## Validation
PASS - 0 CRITICAL, 0 WARNING, 0 INFO

## Key Mapping Decisions
1. **Dual hashes**: `_record_hash` (full row SHA-256) + `_scd2_hash` (address, city, state, county, zip, marital_status) for efficient SCD2 change detection
2. **Bronze column naming**: Preserve source names as-is; renaming at Silver
3. **Deduplication**: Hash-based dedup via SELECT DISTINCT on business key + `_record_hash`
4. **Unmapped codes**: Default to literal 'UNMAPPED' with `_is_unmapped` flag column
5. **Cost null handling**: COALESCE to 0 at Bronze-to-Silver boundary
6. **Surrogate keys**: MD5(natural_key) cast to BIGINT via `CAST(('x' || SUBSTR(MD5(nk), 1, 16)) AS BIGINT)`
7. **SCD2 tracked attributes**: address, city, state, county, zip, marital_status (per DMS SS6)

## Source Tables Verified (DESCRIBE + null rates)
- synthea.patients: 28 columns, 5767 rows; DEATHDATE 86.7% NULL, MARITAL 31.6% NULL, SUFFIX 99.0% NULL, PREFIX 18.2% NULL, RACE 0% NULL
- synthea.encounters: 15 columns, 340532 rows; REASONCODE 36.5% NULL, BASE_ENCOUNTER_COST 0% NULL, PAYER_COVERAGE 0% NULL
- synthea.conditions: 7 columns, 209767 rows
- All 13 Phase 1 tables confirmed in synthea schema

## STM Coverage
- **Source-to-Bronze**: All 13 tables mapped (197+ source columns + metadata columns)
- **Bronze-to-Silver**: 13 silver tables (9 clinical + 3 reference + 1 billing)
- **Silver-to-Gold**: 10 gold tables (4 dims + 3 facts + 3 consumer aggregates)
- **Code Systems**: HL7 Gender, SNOMED-CT encounters, CDC Race/Ethnicity, LOINC, RxNorm
- **Null Handling**: 42 field-level rules with criticality (HIGH/MEDIUM/LOW)
- **Edge Cases**: 16 scenarios covering orphans, future dates, duplicates, schema evolution, overflow, SCD2 race conditions, allergy safety
- **Lineage**: 22 gold-to-source lineage chains

## Open Questions (for next session)
- None currently. STM is complete for Phase 1 scope.
