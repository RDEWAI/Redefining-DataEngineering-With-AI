# LLD Session: 2026-04-17

## What Was Updated

Updated the LLD from **v1.3 → v1.4** (Scenario B: same version folder `outputs/lld/v1/`, new file date).

- **Working file**: `outputs/lld/v1/LLD-2026-04-17-patient-360.md`
- **Backup**: `outputs/lld/v1/LLD-2026-03-23-patient-360.md.bak`
- **Status**: `Updated - Pending Review`
- **Derived artifacts auto-regenerated by PostToolUse hook**:
  - `outputs/lld/v1/config/config-template.yaml`
  - `outputs/lld/v1/dag/dag-definition.yaml`
  - `outputs/lld/v1/dag/dag-pipeline.mmd` (newly generated; satisfies prior INFO item)
  - `outputs/lld/v1/dag/dag-diagram.mmd` (preserved)
  - `outputs/lld/v1/impl-sequence.md`

## Trigger

User: "update the lld as we have updated the inputs"

The change in `inputs/lld/v1/`: a new directory `templates/cookiecutter-chapter/` was added containing a parameterised cookiecutter scaffold for the chapter-5 implementation project (`chapter_name=chapter-5`, `project_name=patient_360`, `python_version=3.12`). This template prescribes a different and more complete project structure than the prior LLD §2.1 had documented.

## Changes Applied (by LLD section)

**Metadata** — bumped Version to 1.4, Last Modified to 2026-04-17, Status to "Updated - Pending Review". Added 3 new metadata rows now required by the validator: Target Scaffold, Chapter, Project Name.

**§1 Design Overview** — added cookiecutter template reference and developer-plugin reference.

**§2.1 Project Structure** — replaced prior structure with the rendered cookiecutter layout. Added module-to-template mapping table showing where every LLD construct lives in the rendered project. Added build/dependency notes (hatchling, ruff, pytest, UV).

Path migrations:
- `src/pipelines/{bronze,silver,gold}/` → `src/patient_360/{bronze,silver,gold}/`
- `src/transforms/`, `src/quality/`, `src/config/`, `src/utils/` → `src/patient_360/utils/` (consolidated)
- `src/config/tables/{table}.yaml` → `airflow/configs/{table}.yml`
- `dags/patient360_hourly_v1.py` → `airflow/dags/patient360_hourly_v1.py`
- `src/quality/rules/{bronze,silver,gold}_rules.yaml` → `dq_rules/{table}.yml` (per-table)
- New: `contracts/{table}.yml` + `contracts/dq/{table}.yml` (DDL + DQ pointers with thresholds)
- New: `ddl/liquibase/changelogs/{table}.xml` (schema migrations)
- New: `_infra/{ci,cd,docker}/` (CI/CD/local infra)
- New: `tests/{bronze,silver,gold}/` (layer-mirrored — replaces `tests/{unit,integration,fixtures}/`)
- New: `pyproject.toml` (hatchling build, UV-managed)

**§2.3 Module Interface Contracts** — updated all module paths; added contract entries for `contracts/`, `dq_rules/`, `ddl/liquibase/`.

**§2.4 Testing Strategy** — moved from `tests/{unit,integration,fixtures}/` to `tests/{bronze,silver,gold}/` with unit/integration distinguished by file naming or pytest markers; added a Contract Tests category for `tests/test_contracts.py`.

**§4.2 DAG Specification** — Bronze TaskGroup factory path updated; per-table Config File column updated to `airflow/configs/{table}.yml`.

**§5 Task Implementation Details** — restructured all three task tables (Bronze, Silver, Gold) to include the new validator-required columns: Task ID | Layer | Module Path | Contract File | DQ Rules File | DAG Task Node | Inputs | Outputs | Transform Ref | DQ Check.

**§7 Configuration Schema** — `ingestion.config_dir` default → `airflow/configs`; replaced `ingestion.dq_rules_file` (monolithic YAML) with `ingestion.dq_rules_dir` (per-table dir); updated `ingestion.spark_submit_class` to `patient_360.bronze.ingestion_runner`.

**§9 Deployment** — added new §9.1 Scaffold Infrastructure Layout describing `_infra/ci/`, `_infra/cd/`, `_infra/docker/`, `ddl/liquibase/`. Renumbered §9.1→9.2, §9.2→9.3, §9.3→9.4, §9.4→9.5. Rewrote §9.3 Promotion Process: GitHub Actions stages aligned with template; Make targets aligned with template `Makefile` (`make dev-setup | test | lint | clean`).

**§11 Upstream Artifact References** — added 2 new rows: cookiecutter template and developer plugin. Updated §9.x cross-references after subsection renumbering.

**§12 Traceability Matrix** — updated NFR-10/11 → §9.4; UC OSS row → §9.5; Local execution + Make row → §9.3 with new Make targets.

**§13 Decision Log** — added Decision 13: Project Scaffold — Cookiecutter Template at `inputs/lld/v1/templates/cookiecutter-chapter/`. Updated Decisions 8, 9, 10 prose for new module paths and per-table `dq_rules/` discovery.

**§14 Version History** — added v1.4 entry summarising all changes.

## Validation

Final result: `All checks passed. No issues found.` (0 critical, 0 warnings, 0 info)

Validator turned up new requirements after the cookiecutter scaffold change which were addressed:
- New required metadata rows: Target Scaffold, Project Name, Chapter
- §5 task tables now require columns: Task ID | Layer | Module Path | Contract File | DQ Rules File | DAG Task Node | Inputs | Outputs | Transform Ref | DQ Check
- §9 must reference scaffold infra dirs `_infra/{ci,cd,docker}/` and `ddl/liquibase/`

## Upstream Artifacts Consumed

- DRD-2026-02-11-patient-360.md (v1.1) — unchanged
- HLD-2026-03-17-patient-360-pipeline-v1.1.md (v1.2) — unchanged
- DMS-2026-03-18-patient-360.md (v1.1) — unchanged
- STM-2026-03-21-patient-360.xlsx (v2.0) — unchanged
- DQS-2026-03-21-patient-360.md (v2.0) — unchanged

## Technical Lead Inputs Consumed

- development-standards.md (v1.0) — unchanged
- infrastructure-specs.md (v1.0) — unchanged
- orchestration-patterns.md (v1.0) — unchanged
- **NEW: `templates/cookiecutter-chapter/`** — full chapter-5 project scaffold with `developer-plugin/` (DAG + CI/CD skills), `src/{project}/{bronze,silver,gold,utils}/`, `airflow/{dags,configs}/`, `contracts/` + `contracts/dq/`, `dq_rules/`, `ddl/liquibase/`, `_infra/{docker,ci,cd}/`, `tests/{bronze,silver,gold}/`, `pyproject.toml` (hatchling + UV), `Makefile`

## Open Items

- Existing open questions from HLD §8 remain (cloud platform, production RTO/RPO, schema evolution strategy).
- The `dq_rules/{table}.yml` per-table rule files referenced in §2.3 / §5 do not yet exist on disk — DQS v2.0 ships monolithic rule files under `outputs/dqs/v2/se-rules/`. A follow-up DQS update or a Phase-2 Bronze framework task should split these into per-table files matching the new convention.
- The Liquibase changelogs referenced in §9.1 (`ddl/liquibase/changelogs/{table}.xml`) do not exist yet — they will be generated during chapter-5 implementation, sourced from DMS §2-§4 schemas.

## No Pending Learnings

`memory/lld/learnings-queue.jsonl` is empty; no user corrections occurred during this session (unambiguous input change → mechanical alignment with the cookiecutter scaffold).
