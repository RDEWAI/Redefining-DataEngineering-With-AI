# LLD Deviations & Adjustments — Silver/Gold Plugin

Tracks every place where the `silver-gold-plugin` skills implement something
differently from the published LLD
(`chapter-4/outputs/lld/v1/LLD-2026-05-12-patient-360.md`). Each item should
either be accepted (LLD edited to match) or rejected (skill changed to match
LLD). Status: **PENDING REVIEW**.

| # | LLD location | LLD says | Plugin does | Reason | Recommended LLD edit |
|---|---|---|---|---|---|
| 1 | §2.3 `src/patient_360/utils/scd2.py` | `apply_scd2(df, natural_keys, hash_columns, effective_date)` — 4 params; output is "Delta MERGE INTO result (rows inserted, rows closed)" | `apply_scd2(df, target_path, natural_keys, hash_columns, effective_date)` — 5 params; returns `{"rows_inserted", "rows_closed", "rows_unchanged"}` | The LLD signature has no `target_path` argument, so the function cannot locate which Delta table to MERGE INTO. The Spark session is derived internally via `df.sparkSession`. | Update §2.3 Input list to include `target_path` (str). Document the returned metrics dict in the Output line. |
| 2 | §5.2 "DQ Check" column | Conflates two gates in one cell: "Inline SE: ... action_if_failed=<fail\|drop\|ignore>; empty-input: <fail\|write_empty>" | Splits into two independent fields in per-table YAML: `empty_input_behavior` (fail / write_empty) and `se_action_if_failed` (fail / drop / ignore) | They are different gates. SE rule violations and empty-source detection are evaluated at different points in the task; mixing them in one config field makes the runtime behavior ambiguous. | Split §5.2 "DQ Check" into two columns: `empty_input_behavior` and `se_action_if_failed`. Update §2.3 per-table YAML schema accordingly. |
| 3 | §5.2 Silver table names | Domain-prefixed (`clinical_patients`, `reference_organizations`, `billing_claims`, …) | Same | Need to verify DMS §3 uses the same convention; if DMS uses bare names (`patients`), the silver-creation skill will fail traceability checks. | Open: confirm DMS §3 alignment. If DMS uses bare names, fix DMS; otherwise the LLD is consistent. |
| 4 | §2.3 DQ rule file naming | `dq_rules/{table}.yml` (no prefix, underscored, `.yml`) | Copies verbatim from `chapter-4/outputs/dqs/v*/se-rules/<table>.yaml` (with `se-rules-` prefix in upstream filenames) | The `dq-engineer-plugin:generate-se-rules` skill emits files prefixed `se-rules-` with `.yaml` extension, but the LLD's runtime convention is unprefixed `.yml`. The silver-creation skill includes a copy + rename step. | Either: (a) standardize the generator to emit `dq_rules/{table}.yml` directly, or (b) document the rename step in LLD §2.3 next to the runtime path convention. Option (a) is cleaner. |
| 5 | §2.3 surrogate-key strategy | Not specified | `xxhash64(natural_key, effective_date)` OR `max(surrogate_key) + row_number()` from target — explicitly NOT `monotonically_increasing_id()` | `monotonically_increasing_id()` is non-deterministic across runs and per-executor — it will produce different surrogate keys on every retry, breaking SCD2 history. | Add a sentence to §2.3 `scd2.py` Responsibility specifying deterministic surrogate-key generation and explicitly forbidding `monotonically_increasing_id()`. |
| 6 | §13 Decision 15 (revised 2026-05-12) — path-based external Delta | "Bronze writes revert to path-based Delta against `warehouse/{env}/bronze/<table>/`" | Pre-fix: `replace_where_write` called `saveAsTable` without `.option("path", ...)`, creating managed tables in the default Spark warehouse dir (`bronze.db/<table>/`) — drift from revised Decision 15. Post-fix (2026-05-20): `.option("path", _external_table_path(table_fqn))` added; writes are external Delta and idempotent across embedded-Derby metastore resets. | The original code followed the pre-revocation Decision 15 (UC-managed). When Decision 15 was revoked on 2026-05-12, the code was not updated. Symptom: `DELTA_CREATE_TABLE_WITH_NON_EMPTY_LOCATION` on every re-run after a container recreation, because Derby forgot the table while the Delta data remained on disk. | None — LLD already specifies the correct contract. Code now matches. Mark Resolved (2026-05-20). |
| 7 | §9.1 — Dockerfile scaffold writability | "Every path the pipeline resolves at runtime — config dirs, DQ rules dirs, warehouse roots, dead-letter/quarantine paths — is computed as `${PATIENT360_PROJECT_ROOT}/<relative-path>`" (implies writability of those paths) | Generated `Dockerfile.airflow` left `/opt/patient_360/` as `root:root drwxr-xr-x`; Docker auto-creates bind-mount-point parents as root. The airflow user (uid 50000) couldn't create the `warehouse/` subdir at runtime. Fix (direct-edit 2026-05-22): added `RUN mkdir -p /opt/patient_360/warehouse && chown -R airflow:root /opt/patient_360 && chmod 775 /opt/patient_360` between the JDK install (`USER root`) and pip install (`USER airflow`) blocks. | Pending retrofit through STORY-01-007 new AC + `update-scaffold` re-run. Add explicit AC: "Dockerfile makes `${PATIENT360_PROJECT_ROOT}` writable by the airflow runtime user before USER switch." Pattern codified in learnings queue for `create-scaffold`. | Recommend LLD §9.1 add a sentence: "Cookiecutter Dockerfile MUST chown PATIENT360_PROJECT_ROOT to the runtime user before USER switch." |
| 8 | §13 Decision 12 (revised 2026-05-12) — UC removed from runtime path | "Spark catalog wires DeltaCatalog + embedded Derby Hive metastore. UC OSS is not in the runtime write path." | Generated `src/patient_360/bronze/reconciliation.py` kept the pre-pivot `uc_uri: str \| None = None` parameter and called `build_spark(app_name=..., uc_uri=uc_uri)`. After Decision 12 was revised, `build_spark` lost the `uc_uri` kwarg. Reconciliation task failed with `TypeError: build_spark() got an unexpected keyword argument 'uc_uri'`. Fix (direct-edit 2026-05-22): dropped the `uc_uri` parameter and stale docstring; call site is now `build_spark(app_name="reconciliation_bronze")`. | Pending retrofit through STORY-02-006 new AC explicitly listing `src/<project>/bronze/reconciliation.py` as a deliverable (so route-out dispatches it to `update-ingestion`). Without this, the file falls through the chain — `update-dag` doesn't touch Python modules; `update-ingestion` only touches files in story-declared ACs. | None — LLD already specifies UC out of runtime; code now matches. Mark Resolved-on-direct-edit pending retrofit. |
| 9 | §3.2 / §9.1 — Silver path anchoring | "Silver SCD2 dimension target path is `${PATIENT360_PROJECT_ROOT}/warehouse/{env}/silver/<domain>/<table>/`" (LLD §9.1 anchor rule) | Generated `src/patient_360/silver/transform_patients.py` returned RELATIVE paths from `_bronze_path()` and `_silver_target_path()` (e.g. `f"warehouse/{env}/bronze/synthea_patients"`). Spark resolved them against spark-submit CWD (`/opt/airflow`), not `PATIENT360_PROJECT_ROOT`. Reads 404'd while Bronze (post-pivot) wrote absolute paths. Same root cause hit `dq_rules_dir = Path(cfg.get("paths.dq_rules", "dq_rules"))` — passed relative path to `run_dq`, which honored the explicit param and skipped the `DQ_RULES_DIR` env-var fallback. Fix (direct-edit 2026-05-22): added `_warehouse_root()` reading `PATIENT360_PROJECT_ROOT`; both path helpers now return absolute paths. For DQ rules: dropped the explicit `dq_rules_dir=` kwarg so `se_runner._resolve_dq_rules_dir` falls back to the `DQ_RULES_DIR` env var (set correctly by docker-compose). | Pending retrofit through STORY-03-001 new AC mandating absolute paths + no-relative-dq_rules-dir + `update-silver` re-run. Pattern codified in learnings queue for `create-silver`/`create-gold`. | None — LLD §9.1 already anchors on PATIENT360_PROJECT_ROOT; code now matches. Recommend LLD §5.2 add an explicit "Silver/Gold path helpers MUST emit absolute paths" sentence to harden the contract. |
| 10 | §2.3 `src/patient_360/utils/scd2.py` (v1.13) | "apply_scd2 performs SCD Type 2 MERGE only — caller runs DQ first; helper trusts every row." Implicit: no catalog DDL, no first-run schema-bootstrap path. | Direct-edited 2026-05-22 to (a) add `target_table: str \| None = None` kwarg, (b) add a first-run cold-warehouse branch that wrote initial Delta AND issued `CREATE TABLE IF NOT EXISTS {target_table} USING DELTA LOCATION '{target_path}'` so `clinical.clinical_patients` would be visible to UC queries after first write. Symptom that drove the edit: `DELTA_MERGE_UNRESOLVED_EXPRESSION` on cold-warehouse first MERGE + UC UI showed no tables (LLD §13 Decision 12 "UI demo only" trade-off). Same trip also direct-edited `transform_patients.py` to issue `CREATE SCHEMA IF NOT EXISTS clinical` + `CREATE TABLE … LOCATION …` for the same UC visibility reason. | The fix conflated two concerns: (1) legitimate cold-warehouse handling (no MERGE target schema yet → write initial Delta path-based — correct), and (2) UC table registration (CREATE TABLE … LOCATION — should be deploy-time, not runtime). Conflation was wrong: runtime writers should never own catalog DDL, especially given Decision 12's UC-out-of-runtime-path stance. | **Resolved 2026-05-23** by LLD v1.18 Decision 17 (after two pivots) + STORY-01-008 v1.14 AC9-AC12 + STORY-03-001 AC8-AC10. Final implementation: deploy-time UC registration via pure-Python REST client (`scripts/bootstrap_uc_tables.py`) against `/api/2.1/unity-catalog/{schemas,tables}`. Runtime `CREATE TABLE` revoked from both `apply_scd2` and `transform_patients.py`; lock-in test added (`test_no_runtime_ddl_in_silver_sources`). `target_table` kwarg removed from `apply_scd2` signature. Cold-warehouse first-run write retained as path-based-only. End-to-end verified: 14 tables registered in UC, idempotent re-run, silver code clean. |
| 11 | LLD v1.18 §2.3 generated `scripts/bootstrap_uc_tables.py` (created by update-scaffold) | Generator emits a script whose `_spark_type` helper (later: `_column_type_spec`) maps only bare type names, and whose SparkSession builder sets `spark.sql.extensions=DeltaSparkSessionExtension` but NOT `spark.sql.catalog.spark_catalog=DeltaCatalog`. | Direct-edited 2026-05-23 during end-to-end bring-up: (a) `_spark_type` extended to pass through parameterized SQL standard types (`VARCHAR(N)`, `CHAR(N)`, `DECIMAL(p,s)`); (b) added `.config("spark.sql.catalog.spark_catalog", "org.apache.spark.sql.delta.catalog.DeltaCatalog")` to the SparkSession builder so Delta's `CREATE TABLE … USING DELTA LOCATION` planner could resolve. Both edits became MOOT a few minutes later when LLD v1.18 pivoted the script away from Spark to a pure-Python REST client (Spark 4 vs UCSingleCatalog 0.3.0 binary-incompat: `NoSuchMethodError: org.apache.spark.internal.LogKey.$init$`). The pure-Python REST client supersedes both helpers entirely. | The v1.17 Spark-DDL path was unbuildable on Spark 4.x — `unitycatalog-spark_2.13:0.3.0` (latest on Maven Central, June 2025) is built for Spark 3.5.x. The Spark-DDL direct-edits surfaced a real generator gap (parameterized types, two-catalog wiring) BUT the entire approach was wrong: any UCSingleCatalog interaction on Spark 4.x fails. LLD v1.18 second pivot removed the entire failure surface. | **Resolved 2026-05-23** by LLD v1.18 Decision 17 (second pivot to REST API). The direct-edited `_spark_type` and DeltaCatalog binding no longer exist in the rewritten script — replaced by `_column_type_spec` (handles parameterized types correctly from the start) and the pure-Python httpx client (no SparkSession at all). No retrofit needed. |

## How to resolve

Treat this file as the canonical agenda for the next LLD update cycle.
For each row:

1. **Decide direction**: accept the plugin behavior (edit LLD) or reject it
   (edit the plugin).
2. **Apply the change**: if LLD edit, use `technical-lead-plugin:update-lld`
   to record the version bump and put the deviation under "Change Log" in
   the new LLD revision.
3. **Update this file**: mark the row Resolved/Rejected with a date and a
   pointer to the LLD revision that closed it.

## Conventions

- Every new deviation MUST be logged here before the skill emits any code
  that conflicts with the LLD.
- Plugins MUST NOT silently deviate from the LLD — if a skill chooses an
  alternate implementation, the rationale belongs in this file.
- This file is reviewed in every Silver/Gold session-end summary.
