# DQ contract pointer for reference_organizations (Silver SCD2 dimension).
# FK reference dimension — referential integrity for clinical/billing facts,
# so PROD action_if_failed: fail (DQS §2 reference_organizations,
# DQ-FLD-095/096/184; LLD §5.2 — Fail task).
table: reference_organizations
layer: silver
dq_rules_path: ../../dq_rules/reference_organizations.yml

# Severity thresholds for reconciliation tasks (LLD §5.5).
thresholds:
  completeness_min: 0.99
  validity_min: 0.995
  freshness_max_hours: 1
